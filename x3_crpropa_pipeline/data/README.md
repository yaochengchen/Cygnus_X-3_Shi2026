# Data directory

Place the real MHD snapshot here, or point `mhd_input.path` to another location.

`demo_mhd.npz` is a (32^3) synthetic periodic cube used only to verify array
names, axis conversion, scaling, and periodic interpolation. It must not be
used for scientific results.
